// Campaign faction used by KdK Vietnam.
//
// Inherit from the base USSR faction instead of USSR_Campaign.conf. The
// latter contributes its own predefined Vanilla groups, which are merged
// into child arrays and cannot be removed by merely deleting overrides.
// The faction key remains USSR; gameplay-specific groups, catalogs and
// loadouts are supplied by CampaignFactionManager.et.
SCR_CampaignFaction : "{09727032415AC39B}Configs/Factions/USSR.conf" {
 FactionKey "USSR"
}
